# vim: set ft=sh:

_local_packages()
{
    pacman -Qq
}

_sync_packages()
{
    pacman -Ssq "$@"
}

_aur_packages()
{
    cower -sq "$@" 2> /dev/null
}

_owl()
{
    COMPREPLY=()

    if [[ $COMP_CWORD -ge 1 ]] ; then
        local current_word="${COMP_WORDS[COMP_CWORD]}"
        case $current_word in
            --*)
                local long_opts='--aur --recursive --cascade --dependencies --quiet --crawl-homes'
                COMPREPLY=( $(compgen -W "$long_opts" -- "$current_word") )
                return 0
                ;;
            -*)
                local short_opts='-a -s -c -d -q -h'
                COMPREPLY=( $(compgen -W "$short_opts" -- "$current_word") )
                return 0
                ;;
        esac
        if [[ $COMP_CWORD -eq 1 ]] ; then
            local actions='update install remove download info page home find search query list listgrep listbin listetc listman grep owner cleanup leftovers foreigns orphans'
            COMPREPLY=( $(compgen -W "$actions" -- "$current_word") )
            return 0
        else
            local second_word=${COMP_WORDS[1]}
            local packages=""
            case $second_word in
                list | listgrep | listbin | listetc | listman | grep | remove)
                    [[ $second_word == grep && $COMP_CWORD -eq 2 ]] && return 0
                    [[ $second_word == listgrep && $COMP_CWORD -eq 2 ]] && return 0
                    packages="$(_local_packages)"
                    ;;
                install | info | home)
                    packages="$(_sync_packages "$current_word"; _aur_packages "$current_word")"
                    ;; 
                download | page)
                    packages="$(_aur_packages "$current_word")"
                    ;; 
                owner)
                    compopt -o filenames
                    COMPREPLY=( $(compgen -f "$current_word") )
                    return 0
                    ;;

            esac
            if [[ -n "$packages" ]] ; then
                COMPREPLY=( $(compgen -W "$packages" -- "$current_word") )
                return 0
            fi
        fi
    fi
}

complete -F _owl owl
